<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Danh sách thống kê sản phẩm</title>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f4f4f4;
        }

        h1 {
            text-align: center;
            padding: 20px 0;
            background-color: #333;
            color: #fff;
        }

        #top-selling-products {
            width: 80%;
            margin: 20px auto;
            border-collapse: collapse;
            box-shadow: 0 0 20px rgba(0, 0, 0, 0.1);
            background-color: #fff;
        }

            #top-selling-products th, #top-selling-products td {
                border: 1px solid #ddd;
                padding: 12px;
                text-align: left;
            }

            #top-selling-products th {
                background-color: #333;
                color: #fff;
            }

            #top-selling-products tbody tr:nth-child(even) {
                background-color: #f9f9f9;
            }

            #top-selling-products tbody tr:hover {
                background-color: #e0e0e0;
            }
    </style>
</head>
<body>

    <h1>Danh sách thống kê toàn bộ sản phẩm bán chạy</h1>

    <table id="top-selling-products">
        <thead>
            <tr>
                <th>Tên Sản Phẩm</th>
                <th>Giá</th>
                <th>Số Lượng đã bán</th>
                <th>Tổng</th>
            </tr>
        </thead>
        <tbody></tbody>
    </table>

    <script>
        $(document).ready(function () {
            $.ajax({
                url: "/Admin/Gembox/GetTopsale",
                type: "GET",
                dataType: "json",
                success: function (data) {
                    var tableBody = $("#top-selling-products tbody");
                    tableBody.empty(); // Clear any existing data

                    $.each(data, function (index, product) {
                        var row = $("<tr>");
                        $("<td>").text(product.name).appendTo(row);
                        $("<td>").text(product.price).appendTo(row);
                        $("<td>").text(product.quantitySold).appendTo(row);
                        $("<td>").text(product.totalRevenue).appendTo(row);
                        row.appendTo(tableBody);
                    });
                },
                error: function (error) {
                    console.error("Error fetching top selling products:", error);
                }
            });
        });
    </script>

</body>
</html>
